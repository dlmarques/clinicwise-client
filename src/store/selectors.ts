export function getUserInfo(state: any) {
  return state.user;
}

export function getUserRole(state: any) {
  return state.user.role;
}

export function getUserTheme(state: any) {
  return state.user.theme;
}
